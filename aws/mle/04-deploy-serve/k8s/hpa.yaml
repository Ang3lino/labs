apiVersion: autoscaling/v1
kind: HorizontalPodAutoscaler
metadata:
  name: fraud-model
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: fraud-model
  minReplicas: 2
  maxReplicas: 10
  targetCPUUtilizationPercentage: 70
